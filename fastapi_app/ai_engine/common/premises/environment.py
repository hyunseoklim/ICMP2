"""
environment — 작업장 환경 상수 (가스·전력 무관 공통 부분).

본 모듈은 시스템 전제 조건서 차원 1(환경 가정)의 *공통 부분*을 구현한다.
가스 전용 환경(E.1 환기 3단계, E.2 가스 분포)은 gas/premises/environment.py에 정의.

본 모듈이 정해주는 것:
    - 작업장 평균 온도 (E.3)
    - 작업장 평균 습도 (E.4)
    - 표준 대기압 (E.5)

본 과업에서는 환경 변동 모델링을 수행하지 않으므로 *상수만 정의*한다.
후속 운영에서 *센서 보정 함수*가 본 상수들을 참조할 수 있도록 자리표시자로 유지.
"""

from __future__ import annotations


# ============================================================================
# E.3 작업장 온도
# ============================================================================

WORKSPACE_TEMPERATURE_C: float = 22.0
"""작업장 평균 온도 (섭씨). 결정 E.3."""

WORKSPACE_TEMPERATURE_STD_C: float = 1.0
"""작업장 온도 표준편차 (섭씨). ±1℃ 변동 범위."""


# ============================================================================
# E.4 작업장 습도
# ============================================================================

WORKSPACE_HUMIDITY_RH: float = 50.0
"""작업장 평균 상대 습도 (% RH). 결정 E.4."""

WORKSPACE_HUMIDITY_STD_RH: float = 5.0
"""작업장 습도 표준편차 (% RH). ±5% 변동 범위."""


# ============================================================================
# E.5 표준 대기압
# ============================================================================

STANDARD_PRESSURE_KPA: float = 101.325
"""표준 대기압 (kPa). 결정 E.5. 1 atm = 101.325 kPa."""

STANDARD_PRESSURE_HPA: float = 1013.25
"""표준 대기압 (hPa). kPa의 10배."""
