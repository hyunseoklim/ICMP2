[pytest]
testpaths = gas/tests power/tests devkit/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    slow: 학습이 포함된 느린 테스트 (실행 시 수 초 이상)

# 기본은 slow 포함 실행
# 빠른 검증만: pytest -m "not slow"
addopts = -v --tb=short
